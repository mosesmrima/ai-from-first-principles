{
  "name": "ai-tracker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "seed:gen": "node seed/generate-seed.mjs",
    "db:reset:local": "wrangler d1 execute ai-tracker --local --file=./schema.sql && wrangler d1 execute ai-tracker --local --file=./seed/seed.sql",
    "db:reset:remote": "wrangler d1 execute ai-tracker --remote --file=./schema.sql && wrangler d1 execute ai-tracker --remote --file=./seed/seed.sql",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.107.0",
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.6.0"
  }
}
