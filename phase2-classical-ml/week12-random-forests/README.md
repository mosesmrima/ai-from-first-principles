# Week 12 — Random Forests

> **▶ Your ordered steps live in the tracker app → https://ai-tracker.mrima.workers.dev (open "Now"). This README is reference.**

**Build:** a random forest on top of your Week 11 tree — bootstrap sampling (bagging) + random
feature subsets per split + majority vote. Compare accuracy and variance against a single tree.

**Resources:** StatQuest (random forests, bagging, boosting); ISLR ch. 8.

**Checkpoint:** explain how bagging + feature randomness reduce variance, and the gist of how
boosting differs. Mark done: `uv run python track.py done week12`.
