# Week 9 — Linear Regression from Scratch

**Build:** linear regression both ways — the closed-form normal equation
`w = (XᵀX)⁻¹ Xᵀy` **and** gradient descent — and confirm they agree.

**Resources:** CS229 lectures 1–2; ISLR ch. 3. Reuse your Week 8 `LinearRegression`.

**Checkpoint:** explain when the closed form is preferable to gradient descent (and when it isn't —
huge or singular `XᵀX`). Mark done: `uv run python track.py done week09`.
