# Week 8 — Capstone: Linear & Logistic Regression (NumPy only)

**Artifact:** `linear_regression.py`, `logistic_regression.py` — implement both models, their loss functions, and gradient-descent training yourself (no sklearn).

**Do the steps in the app** → open **Now** at https://ai-tracker.mrima.workers.dev
(exact videos, time estimates, and the order live there — this README is just a local reference).

**Verify:** `uv run pytest phase1-math/week08-capstone-regression/`

**Checkpoint:** Explain, without notes: the linear-regression loss (MSE) and its gradient w.r.t. the weights; why logistic regression uses the sigmoid + log-loss instead of MSE; and what one step of gradient descent does to the weights.
