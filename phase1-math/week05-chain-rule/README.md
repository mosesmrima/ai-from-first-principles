# Week 5 — Partial Derivatives & the Chain Rule

**Artifact:** `manual_backprop.py` — take a tiny computation graph, compute every partial derivative by hand, then verify numerically with finite differences.

**Do the steps in the app** → open **Now** at https://ai-tracker.mrima.workers.dev
(exact videos, time estimates, and the order live there — this README is just a local reference).

**Checkpoint:** Hand-derive backprop for a 2-layer network (one hidden layer) and confirm each gradient matches a finite-difference check to ~1e-6.
